export type FindAndNameItem = {
  id: string;
  image: string; // URL o asset local para trabajar offline
  label: string; // pista para el terapeuta / atributo alt
  correctOption: string;
  options: string[];
};
